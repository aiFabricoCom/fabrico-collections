interface:
  display_name: "Fabrico Implement Common Task"
  short_description: "Run the implement common task workflow with Fabrico"
  default_prompt: "Use $fabrico-collections:fabrico-implement-common-task to run this workflow for my request."

policy:
  allow_implicit_invocation: false
