interface:
  display_name: "Fabrico Create Workflow"
  short_description: "Create a reusable Fabrico entry workflow"
  default_prompt: "Use $fabrico-collections:fabrico-create-workflow to create a new Codex entry workflow skill."

policy:
  allow_implicit_invocation: false
