interface:
  display_name: "Fabrico Create Spec"
  short_description: "Run the create spec workflow with Fabrico"
  default_prompt: "Use $fabrico-collections:fabrico-create-spec to run this workflow for my request."

policy:
  allow_implicit_invocation: false
