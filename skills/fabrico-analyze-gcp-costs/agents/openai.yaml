interface:
  display_name: "Fabrico Analyze Gcp Costs"
  short_description: "Run the analyze gcp costs workflow with Fabrico"
  default_prompt: "Use $fabrico-collections:fabrico-analyze-gcp-costs to run this workflow for my request."

policy:
  allow_implicit_invocation: false
