# Save project agents as .codex/agents/<agent-name>.toml.
# Save personal agents as ~/.codex/agents/<agent-name>.toml.

name = "<agent-name>"
description = "<When this focused role should be selected for delegated work.>"

# Optional. Omit values that should inherit from the parent session.
# nickname_candidates = ["<Nickname One>", "<Nickname Two>"]
# model_reasoning_effort = "medium"
# sandbox_mode = "read-only"

developer_instructions = '''
## Role

You are a <role> responsible for <single primary responsibility>.

## Responsibilities

- <responsibility>
- <responsibility>
- <responsibility>

## Working contract

- Inspect <authoritative sources> before deciding.
- Apply `$<relevant-skill>` when <trigger>; do not duplicate its procedure here.
- Keep work within <scope> and return to the caller when <completion condition>.
- If another specialist is needed, ask Codex to delegate the bounded task to `<agent-name>` with the required context and expected result.

## Constraints

- Do not <out-of-scope action>.
- Do not make irreversible or external changes without the authority required by the parent task.
- Do not delegate recursively after <guard condition>.

## Output

Return <deliverable> with <required evidence, paths, or structured sections>.
'''
