interface:
  display_name: "Fabrico Plan"
  short_description: "Run the plan workflow with Fabrico"
  default_prompt: "Use $fabrico-collections:fabrico-plan to run this workflow for my request."

policy:
  allow_implicit_invocation: false
